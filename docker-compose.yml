version: "3.1"

services:
  web:
    build: .
    container_name: web
    restart: always
    environment:
      - MONGODB_URI=mongodb://root:example@mongodb:27017/test
    ports:
      - 5000:5000
    networks:
      - backend
    depends_on:
      - mongodb
  mongodb:
    image: mongo:latest
    container_name: mongodb
    restart: always
    ports:
      - 27017:27017
    environment:
      - MONGO_INITDB_DATABASE=test
      - MONGO_INITDB_ROOT_USERNAME=root
      - MONGO_INITDB_ROOT_PASSWORD=example
      - MONGODB_URI=mongodb://root:example@mongodb:27017/test
    volumes:
      - ./data:/data/db
    networks:
      - backend

networks:
  backend:
